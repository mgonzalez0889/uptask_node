<!DOCTYPE html>
html(lang="en")
    head
        include partial/header
    body.login
        .contenedor-formulario  
            h1 Crea tu Cuenta en Uptask

            if locals.mensajes 
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error                

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") Email:
                    input(type="text" name="email" placeholder="Tu email" autocomplete="off" value= email )
                .campo    
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu password" autocomplete="off" value= password)
                .campo    
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar Sesion

        script(src="/dist/bundle.js")                

